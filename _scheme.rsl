Hard {
    comp: u16,
    valv : u16,
    i_red: u8,
    i_inf: u8,
}

Info {
    version: str,
}

Build {
    version : str,
    compiler: str,
    git: str,
    time: str,
    target: str,
    host: str,
    profile: str,
    authors: str,
}